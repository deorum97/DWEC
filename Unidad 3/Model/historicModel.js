class Historico {
  constructor(valueOrigin, valueExchanged, divisaFrom, divisaTo, fechaActual) {
    (this.valueOrigin = valueOrigin),
      (this.valueExchanged = valueExchanged),
      (this.divisaFrom = divisaFrom),
      (this.divisaTo = divisaTo),
      (this.fechaActual = fechaActual);
  }
}
